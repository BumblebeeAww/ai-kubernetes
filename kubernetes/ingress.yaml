apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ai-tonality-ingress
spec:
  rules:
    - http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: ai-tonality-service
                port:
                  number: 80